pipeline {

    agent any
    
    stages{
        stage('build'){
            steps{
                    echo "inside build"
                  }
         }
         stage('Deploy'){
             steps{
                     echo "deploy"
                   }
         }
       }
     }                           

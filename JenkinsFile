pipeline {

    agent any
    
    stages{
        stage('Build'){
            steps{
                    echo "inside build"
                    'mvn clean package'
                  }

            post{
              success{
                echo "now archiving"
                archiveArtifacts artifacts: '**/target/*.jar'
              }
            }      
         }
        
       }
     }                           

pipeline {

    agent any
    
    stages{
        stage('build'){
            steps{
                    print "inside build"
                  }
         }
         stage('Deploy'){
             steps{
                     print "deploy"
                   }
         }
       }
     }                           